version: "3.9"

services:
  django:
    build: .
    container_name: django_app
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - DEBUG=True

    volumes:
      - .:/app
    ports:
      - "8000:8000"